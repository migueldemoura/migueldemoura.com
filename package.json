{
  "name": "migueldemoura.com",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "normalize-scss": "^7.0.1",
    "turbolinks": "^5.1.1",
    "zooming": "^2.1.0"
  },
  "devDependencies": {
    "chart.js": "^2.7.2",
    "stylelint": "^9.1.3",
    "stylelint-scss": "^2.5.0",
    "stylelint-config-recommended": "^2.1.0"
  },
  "scripts": {
    "setup": "bundle install --path ruby_gems && npm install",
    "serve": "bundle exec jekyll serve",
    "clean": "rm -rf package-lock.json Gemfile.lock node_modules/ _site/ .bundle/ .jekyll-cache/ ruby_gems/"
  }
}
