#!/usr/bin/env bash

cd "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"/../

case "$1" in
    "setup")
        bundle install
        yarn install ;;
    "serve")
        bundle exec jekyll serve ;;
    "clean")
        rm -f yarn.lock Gemfile.lock
        rm -rf .jekyll-cache/ node_modules/ _site/ ;;
    *) exit -1 ;;
esac
